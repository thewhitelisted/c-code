#include <stdio.h>
#include <math.h> // required

int main()
{
    double A = sqrt(9); // A = square root of 9
    double B = pow(2, 4); // B = 2 to the power of 4
    int C = round(3.14); //  rounds a number
    int D = ceil(3.14); // rounds a number up
    int E = floor(3.99); // rounds a number down
    double F = fabs(-100); // absolute value
    double G = log(3); // logarithm of the number
    double H = sin(45); // sine of a number in radians
    double I = cos(45); // cosine of a number in radians
    double J = tan(45); // tangent of a number in radians

    return 0;
}